using System.Windows;

namespace GpibUtils.Wpf
{
    /// <summary>The shell's main window. All behaviour lives in the bound view model (MVVM).</summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
